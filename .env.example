# LogHive - Environment Variables Template
# Copy this file to .env and fill in your actual values

# ==================== Server Configuration ====================

# Server port (default: 5100)
PORT=5100

# Environment: 'production' or 'test'
ENVIRONMENT=production

# ==================== Security Settings ====================

# Flask secret key (MUST change in production!)
# Generate with: python -c 'import secrets; print(secrets.token_hex(32))'
SECRET_KEY=your-secret-key-here-change-me

# API token for agents (MUST change in production!)
# Generate with: python -c 'import secrets; print(secrets.token_urlsafe(32))'
API_TOKEN=your-api-token-here-change-me

# ==================== User Accounts ====================

# Admin account credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me-in-production

# Test account credentials
TEST_USERNAME=test
TEST_PASSWORD=change-me-in-production

# ==================== Virtual Environment ====================

# Path to virtual environment (optional, used by deploy/start.sh)
# VENV_PATH=/opt/loghive/venv

# ==================== Grafana (EC2 #1) ====================

# Grafana admin credentials (default: admin/admin)
GF_ADMIN_USER=admin
GF_ADMIN_PASSWORD=admin

# ==================== Agent Machine (EC2 #2) ====================

# Central server URL for agents
# CENTRAL_SERVER_URL=http://<EC2-1-Elastic-IP>:5100/api/report

# File generation interval in seconds (default: 86400 = 1 day, demo: 60)
# FILE_GEN_INTERVAL=86400

# Agent report interval in seconds (default: 3600 = 1 hour, demo: 30)
# REPORT_INTERVAL=3600

# File size range for generated files
# MIN_FILE_KB=1          # Minimum file size (KB)
# MAX_FILE_KB=1024       # Maximum file size (KB) - default 1MB, was 20MB
# LARGE_FILE_PROB=30     # % chance of large file (midpoint~MAX); rest are small (MIN~midpoint)
# Monitoring - EC2 #2 (Agent Machine) Private IP
# EC2_AGENT_IP=your-ec2-2-private-ip